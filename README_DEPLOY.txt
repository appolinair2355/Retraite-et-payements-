ASSISNT PAYEMENT — Bot Telegram Gestionnaire d Acces
Deploiement Render.com | Port 10000
=====================================================

VARIABLES OBLIGATOIRES (Render → Environment) :
  BOT_TOKEN  = Token de votre bot (@BotFather)
  PORT       = 10000

VOTRE ID (1190237801) EST INTEGRE — vous etes admin
sans configurer ADMINS.

TELETHON — INTEGRE (API_ID et API_HASH codes) :
  Demarrez le bot → envoyez /connect → entrez votre
  numero de telephone → entrez le code SMS.
  La session est sauvegardee automatiquement.

CLES IA (optionnelles, ajoutables via /setaikey) :
  GEMINI_API_KEYS   OPENAI_API_KEYS   GROQ_API_KEYS
  DEEPSEEK_API_KEYS OCR_SPACE_API_KEY

FONCTIONNEMENT :
  Zero canal au demarrage. Ajoutez le bot comme
  ADMINISTRATEUR dans vos canaux — detection auto.
